<?php

class Item {
    // ------------------------------------------------ private variables
    private $_text;
    private $_category;
    // ------------------------------------------------ contructor
    public function __construct($myText, $myCategory) {
        $_text = $myText;
        $_category = $myCategory;
    }
    // ------------------------------------------------ gets / sets
    // ------------------------------------------------ private methods
    // ------------------------------------------------ public methods
}